**Chip** — filter/category pill with a toggled `selected` state. Genre filters, browse categories, source-type pickers.

```jsx
<Chip selected>All</Chip>
<Chip icon={<FilmIcon/>}>Movies</Chip>
<Chip dot="var(--health-stable)">Live</Chip>
```
