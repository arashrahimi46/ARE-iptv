**TextField** — labeled input. Use `mono` + `prefix` for playlist URLs and Xtream credentials.

```jsx
<TextField label="M3U playlist URL" mono prefix="http://" placeholder="host:port/get.php?..." />
<TextField label="Username" mono value="john_doe" />
<TextField label="Portal name" placeholder="Living room" error="Name already exists" />
```
