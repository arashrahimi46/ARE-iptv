*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
# Module build outputs -- only the root /build was covered, so :tv/:mobile/:core/:baselineprofile
# outputs showed up as untracked noise on every status.
**/build/
/.idea/
/.kotlin/
/captures
.externalNativeBuild
.cxx
local.properties

# Release signing (release-manager: never commit real keystores/credentials)
*.jks
*.keystore
!debug.keystore
keystore.properties

# Agent/orchestration runtime state (logs, sessions, memory dbs) — not app source
.monomind/
.swarm/
.monograph/

# Generated reports / org runtime event logs — not app source
/GRAPH_REPORT.md
/data/
