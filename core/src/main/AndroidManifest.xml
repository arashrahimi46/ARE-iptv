<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Deliberately empty. :core carries string resources only: no code, no permissions.
         Both :tv and :mobile declare INTERNET (and :tv ACCESS_NETWORK_STATE) themselves. -->
</manifest>
